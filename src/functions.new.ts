/**
 * Logs a 'wow' message to the console.
 * @param message - The message to be logged to the console. If not provided, defaults to 'wow'.
 * @returns {void} This function doesn't return a value.
 */
export const wow = (message: string = 'wow') => {
    console.log(message);
}
